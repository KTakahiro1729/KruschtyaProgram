{
  "name": "kruschtya-program",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "pages:dev": "wrangler pages dev frontend --compatibility-date=2024-05-01"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.2.1",
    "hono": "^4.4.7",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "typescript": "^5.4.5",
    "wrangler": "^3.58.0"
  }
}
